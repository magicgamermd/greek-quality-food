/**
 * Складът иска да вижда ВСИЧКО на един екран — без страници.
 *
 * Списъците (продукти, партньори, наличности, доставчици) се дърпат и
 * показват изцяло; браузърът скролва, вместо потребителят да цъка
 * „Напред". Търсачките и филтрите работят върху пълния набор, така че
 * резултат не може да „изчезне" на трета страница.
 *
 * Числото е предпазен клапан, не бизнес ограничение — огледало е на
 * MAX_PAGE_SIZE в бекенда (warehouse-backend/src/lib/pagination.ts).
 */
export const SHOW_ALL_LIMIT = 25000;
