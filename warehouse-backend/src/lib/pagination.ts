/**
 * Складът работи на един екран: списъците се показват ИЗЦЯЛО, без
 * страници (виж warehouse-frontend/src/lib/pagination.ts).
 *
 * Затова таванът на `?limit=` е висок — той е предпазен клапан срещу
 * абсурден вход, а не бизнес ограничение. Реалните списъци са в
 * порядъка на хиляди реда (каталог ~1800, партньори ~400).
 *
 * Подразбиращата се страница остава малка: клиент, който НЕ поиска
 * изрично голям limit, не дърпа целия каталог.
 */
export const MAX_PAGE_SIZE = 25000;
export const DEFAULT_PAGE_SIZE = 50;
