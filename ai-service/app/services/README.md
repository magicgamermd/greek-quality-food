# Services

This directory is reserved for shared service utilities such as:

- `openai_client.py` — shared OpenAI client singleton
- `storage.py` — file upload handling
- `redis_client.py` — shared Redis client

Add as needed during feature expansion.
